name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: mattfishman.com
        AWS_ACCESS_KEY_ID: AKIA2MTMZKZBBW2BFTZM
        AWS_SECRET_ACCESS_KEY: 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
        AWS_REGION: 'us-east-1'   # optional: defaults to us-east-1
        SOURCE_DIR: '/dist/PersonalWebsite'      # optional: defaults to entire repository
